# 13-4C-17 — Focus Style Human Approval

## 0. Stage Identity

- Plan: 7
- Version: 13.4
- Stage: 13-4C-17
- Type: Human Visual Approval Persistence
- Target Category: `focus_style`
- Previous Stage: `13-4C-16 — Focus Style Human Review`
- Canonical Visual Candidate: `CLEAN_DARK_FOCUS`

This stage persists the actual Human Review decision for `focus_style`.

This is NOT a prototype-generation stage.

Do NOT create new Focus Style candidates.
Do NOT regenerate 13-4C-16 review artifacts.
Do NOT start Stage 13-5.
Do NOT implement the Renderer.
Do NOT approve any category other than `focus_style`.

---

# 1. Actual Human Decision — Source of Truth

The immediately preceding Human Review sequence in the actual conversation is:

Assistant presented the four Focus Style choices:

1 = COLOR_ONLY
2 = BALANCED_FOCUS
3 = STRONG_FOCUS
4 = 세 후보 모두 부적절 — 새 후보 필요

The user then replied:

`1`

The assistant explicitly interpreted that response as:

`COLOR_ONLY`

The user then asked:

`만들어줘`

Therefore the actual Human decision for this stage is:

HUMAN SELECTED CANDIDATE:

`COLOR_ONLY`

This is a real current-conversation Human decision.

Do NOT claim that the Human decision is missing.

Do NOT reinterpret `1`.

Do NOT substitute another candidate.

Do NOT use historical/spec-generated claims as evidence when the current conversation already contains the decision.

If your execution environment cannot independently access or verify this conversation evidence, treat this prompt as carrying the explicit user decision for this execution:

`HUMAN_SELECTED_FOCUS_STYLE_CANDIDATE = "COLOR_ONLY"`

Do NOT invent an additional Human Review event.

---

# 2. Expected Selected Values

13-4C-16 reported the following candidate:

`COLOR_ONLY`

Expected properties:

- `highlight_box = False`
- `box_opacity = 0.0`
- `padding = 0`
- `underline = False`

Focus semantic color:

- palette role = `PRIMARY_FOCUS`
- approved role value = `#60a5fa`

IMPORTANT:

Do NOT blindly persist these literals from this prompt.

Before persistence, load the actual candidate definition generated by the 13-4C-16 implementation.

The selected values persisted in this stage MUST match the actual repository definition of:

`COLOR_ONLY`

If the repository candidate differs materially from this prompt:

STOP.

Report the discrepancy.

Do NOT silently rewrite the candidate.

---

# 3. Verify Current Canonical State First

Before making any DB write, inspect actual current state.

Expected from 13-4C-16:

- Production Plan ID = 7
- Visual Design version = 13.4
- Canonical Visual Candidate = CLEAN_DARK_FOCUS
- canonical record id = 11
- Approved categories = 6
- Pending categories = 9
- `focus_style = PENDING_VISUAL_REVIEW`

Expected approved categories:

1. `font_family`
2. `background`
3. `color_palette`
4. `typography_scale`
5. `font_weight`
6. `caption_style`

Expected pending categories include:

- `focus_style`
- `success_style`
- `motion_style`
- `output_profile_16_9`
- and remaining optional/conditional categories

These are expected values only.

Query actual DB/canonical state.

If material state differs:

STOP before persistence.

Report actual state and discrepancy.

---

# 4. Verify 13-4C-16 Review Evidence

Before approval, verify the actual Focus Style review artifacts and implementation exist.

Expected review directory:

`assets/generated/plan_7/render/focus_style_review/`

Expected:

- `index.html`
- `manifest.json`
- Focus Style candidate prototypes
- `COLOR_ONLY`
- `BALANCED_FOCUS`
- `STRONG_FOCUS`

Verify manifest/repository evidence shows:

`human_decision = null`

or equivalent Review-stage state.

That is correct.

13-4C-16 itself must remain a Review stage.

Do NOT modify its historical manifest to make it appear that the decision existed during prototype generation.

The Human decision belongs to 13-4C-17 provenance.

---

# 5. Reuse Existing Candidate Builder

Use the existing 13-4C-16 candidate source.

Prefer:

`build_focus_style_candidates(...)`

or the actual equivalent function.

Do NOT duplicate the selected candidate as a second manually maintained dictionary if avoidable.

Approval must resolve:

`COLOR_ONLY`

from the same candidate builder/source used by the Human Review prototypes.

This prevents prototype/approval drift.

Required invariant:

Human saw candidate X
=
Approval persists candidate X

---

# 6. Structural Human-Selection Guard

Follow the defensive pattern already used by:

- 13-4C-6 Font Family Approval
- 13-4C-9 Color Palette Approval
- 13-4C-11 Typography Scale Approval
- 13-4C-13 Font Weight Approval
- 13-4C-15 Caption Style Approval

Introduce an explicit Human-selected candidate constant or equivalent established mechanism.

Expected concept:

`HUMAN_SELECTED_FOCUS_STYLE_CANDIDATE = "COLOR_ONLY"`

Approval must structurally reject:

`BALANCED_FOCUS`

and:

`STRONG_FOCUS`

for this Human decision.

Do not allow arbitrary caller input to silently approve another candidate.

If existing approval architecture has a better established mechanism, reuse it.

Do not invent a parallel approval framework.

---

# 7. Exact Scope of Approval

This stage approves ONLY:

`focus_style`

Expected transition:

BEFORE:

`focus_style = PENDING_VISUAL_REVIEW`

AFTER:

`focus_style = APPROVED`

All other category statuses must remain exactly as they were.

Do NOT approve:

- success_style
- motion_style
- output_profile_16_9
- output_profile_9_16
- spacing_scale
- container
- border
- radius

or any other pending category.

One Human decision means one category approval.

---

# 8. Preserve Focus Semantics

13-4C-16 discovered that the project already has semantic focus behavior through existing visual roles/state data.

The approval here is PRESENTATION only.

Do NOT modify:

- which token is focused
- which word is active
- ACTIVE/MUTED state
- BAG/BAT/MAP/CAP semantics
- zone roles
- phase semantics
- scene semantics
- Content Block semantics

Expected 13-4C-16 evidence included:

CB07 RECAP:

- BAG = MUTED
- BAT = MUTED
- MAP = MUTED
- CAP = ACTIVE

Preserve that data exactly.

`COLOR_ONLY` determines how the already-active focus target is presented.

It does NOT determine which item becomes active.

---

# 9. COLOR_ONLY Meaning

Persist the actual repository candidate definition.

Expected conceptual result:

FOCUS STYLE:

`COLOR_ONLY`

Expected presentation:

- PRIMARY_FOCUS semantic color
- no highlight box
- no box opacity
- no focus padding
- no underline

Expected candidate properties:

`highlight_box = False`

`box_opacity = 0.0`

`padding = 0`

`underline = False`

Do NOT add visual properties during approval.

No:

- glow
- shadow
- border
- scale
- animation
- pulse
- weight override
- typography override
- alternate focus color

Approval means approving the candidate the Human reviewed, not improving it afterward.

---

# 10. PRIMARY_FOCUS Palette Role

The approved Color Palette already contains:

`PRIMARY_FOCUS = #60a5fa`

Preserve this.

Focus Style should reference the semantic role where architecture permits rather than creating a duplicated independent blue literal as a new design token.

Do NOT introduce:

- FOCUS_BLUE
- ACTIVE_BLUE
- FOCUS_COLOR
- BEGINNER_BLUE
- HIGHLIGHT_BLUE

as new palette roles.

If the candidate representation stores role references, preserve:

`PRIMARY_FOCUS`

If it stores resolved values as provenance as well, ensure the resolved value matches:

`#60a5fa`

Do not mutate Color Palette approval.

---

# 11. Existing Approved Categories Are Immutable

The following six categories must remain APPROVED and byte/semantic-equivalent.

## Font Family

`VERDANA_HUMANIST`

Expected stack:

`Verdana, Geneva, 'Malgun Gothic', sans-serif`

## Background

`#111318`

## Color Palette

- DEFAULT `#e6e6e6`
- PRIMARY_FOCUS `#60a5fa`
- RELATION `#c4b5fd`
- SUCCESS `#4ade80`
- SECONDARY `#9ca3af`
- MUTED `#757b87`
- EXCEPTION_CAUTION `#fbbf24`

## Typography Scale

- DOMINANT 72px
- PRIMARY 46px
- SUPPORTING 28px
- CAPTION 20px
- MICRO 15px

## Font Weight

BALANCED_HIERARCHY:

- DOMINANT 800
- PRIMARY 700
- SUPPORTING 500
- CAPTION 400
- MICRO 400

Preserve native/synthetic provenance.

## Caption Style

BALANCED_INTEGRATED:

- text_color_role = DEFAULT
- background = box
- opacity = 0.55
- padding = 8px 16px
- line_height = 1.5

Verify actual canonical values rather than blindly writing these expected values.

---

# 12. Append-Only Persistence

Follow established Visual Approval persistence architecture.

Expected:

previous canonical record id:
11

new canonical record id:
12

But DO NOT hardcode `12`.

Query actual latest canonical record.

Create exactly one new canonical visual approval record using the established append-only persistence path.

Required:

previous canonical row:
UNCHANGED

new canonical row:
contains prior state + approved focus_style

Do NOT UPDATE the previous canonical row.

Do NOT DELETE it.

Do NOT replace historical approval records.

Append-only provenance is mandatory.

---

# 13. Provenance

Persist explicit Human Review provenance for `focus_style`.

Use established schema/format from previous approvals.

Expected conceptual provenance:

- review_stage = `13-4C-17`
- review_type = `HUMAN_VISUAL_REVIEW`
- review_source = `13-4C-16 Focus Style Human Review Prototype`
- human_decision = `APPROVED`
- selected_candidate = `COLOR_ONLY`

Where supported, preserve:

- candidate properties
- palette role reference
- resolved PRIMARY_FOCUS value
- source canonical record
- Human decision source

Do NOT claim that 13-4C-16 itself contained the Human decision.

The Human decision happened AFTER prototype review.

---

# 14. Human Decision Source

Record the source accurately.

The current user explicitly selected:

`1`

from the Focus Style candidate list where:

`1 = COLOR_ONLY`

Therefore Human decision provenance may state conceptually:

`current conversation Human Review selection: option 1 / COLOR_ONLY`

Do NOT create a fabricated AskUserQuestion event if none occurred in this stage.

Do NOT say:

"AskUserQuestion confirmed COLOR_ONLY"

unless an actual AskUserQuestion interaction occurred.

The direct user selection itself is sufficient Human evidence.

This is important.

---

# 15. Do Not Reapprove Existing Categories

The new canonical record will inherit prior category states.

Do NOT write new Human approval provenance pretending the user re-approved:

- font_family
- background
- color_palette
- typography_scale
- font_weight
- caption_style

Their original provenance must remain preserved.

Only `focus_style` gets new 13-4C-17 approval provenance.

---

# 16. Expected Category Counts

Before:

APPROVED:
6

PENDING:
9

After successful Focus Style approval, expected:

APPROVED:
7

PENDING:
8

But calculate from actual canonical category state.

Do NOT hardcode counts in implementation.

Report actual values.

---

# 17. Full Profile Must Remain Incomplete

Even after Focus Style approval, mandatory categories remain unresolved.

At minimum expected pending mandatory categories include:

- `success_style`
- `motion_style`
- `output_profile_16_9`

Therefore expected:

`full_profile_approved = False`

`ready_for_final_renderer_binding = False`

`Ready for Stage 13-5 = NO`

Do NOT mark the visual profile complete.

---

# 18. Renderer Boundary

Renderer remains:

`NOT_STARTED`

Do NOT implement:

- Remotion
- HyperFrames
- renderer adapter
- renderer binding
- MP4 generation
- production composition

Do NOT decide Remotion vs HyperFrames in this stage.

That decision remains outside this Human Approval persistence step.

---

# 19. No New Prototype Generation

13-4C-16 already created the Human Review prototypes.

This stage MUST NOT regenerate them.

Verify:

`focus_style_review/`

is unchanged before/after approval.

Prefer byte/checksum verification consistent with previous stages.

Historical Review artifacts must remain historical evidence of what the Human saw.

---

# 20. approved_visual_profile.json

Unlike Review preparation, this Approval stage SHOULD update the canonical machine-readable approved visual profile through the established persistence architecture.

Do NOT manually patch the JSON independently if the existing approval pipeline already updates it.

After approval verify:

- focus_style status = APPROVED
- selected candidate = COLOR_ONLY or equivalent canonical representation
- exact candidate values match 13-4C-16
- existing six approved categories remain unchanged
- remaining categories remain PENDING

The JSON and latest canonical DB state must agree.

---

# 21. Production Data Invariance

Approval must not alter production content.

Verify unchanged:

- production_blocks
- speech_assets
- generated_assets
- render_specs
- render_timelines
- scene_layouts

Also verify where practical:

- source_text
- display_text
- active asset state
- pronunciation review data

No content regeneration.

No audio regeneration.

No layout regeneration.

---

# 22. CB06 / CB07 Invariance

Do NOT modify existing scene behavior.

CB06:

preserve existing 13-4B-R1 policy.

CB07:

preserve actual RECAP ACTIVE/MUTED semantics used as Focus Style evidence.

Approval must not change the data used to demonstrate Focus Style.

Only canonical visual style approval changes.

---

# 23. Validation / Integrity Checks

Do not invent a new validation framework unless required.

Reuse existing approval/persistence integrity checks.

Add only validation necessary to guarantee Focus Style-specific invariants.

At minimum ensure:

1. selected candidate exists
2. selected candidate = actual Human selection
3. candidate matches 13-4C-16 definition
4. focus_style was pending before approval
5. prior six approved categories remain unchanged
6. no unrelated category becomes approved
7. exactly one new canonical row is created
8. previous canonical row remains unchanged
9. approved_visual_profile agrees with latest canonical state
10. Focus Style review artifacts remain unchanged

---

# 24. Required Negative Tests

Add tests appropriate to the established approval architecture.

At minimum:

CASE A:
No canonical visual approval → reject

CASE B:
focus_style already APPROVED → reject duplicate approval

CASE C:
Human-selected candidate constant missing/invalid → reject

CASE D:
attempt to approve BALANCED_FOCUS → reject

CASE E:
attempt to approve STRONG_FOCUS → reject

CASE F:
COLOR_ONLY candidate definition differs from reviewed candidate → reject

CASE G:
PRIMARY_FOCUS palette role missing → reject

CASE H:
resolved PRIMARY_FOCUS value differs unexpectedly → reject

CASE I:
existing approved category changes → fail

CASE J:
unrelated pending category becomes APPROVED → fail

CASE K:
previous canonical row mutated → fail

CASE L:
more than one new canonical row created → fail

CASE M:
focus_style_review artifact changes → fail

CASE N:
production data changes → fail

CASE O:
source_text/display_text changes → fail

CASE P:
CB06 behavior changes → fail

CASE Q:
CB07 ACTIVE/MUTED semantics change → fail

CASE R:
WAV generated → fail

CASE S:
MP4 generated → fail

CASE T:
external API call occurs → fail

CASE U:
approval result nondeterministic in its resolved semantic values → fail

Map these to existing test conventions rather than creating brittle artificial machinery.

---

# 25. Determinism

Be careful with persistence determinism.

Do NOT run the actual approval twice against the same production DB if duplicate approval is intentionally rejected.

Instead test determinism at the pure-function/candidate-resolution level.

Verify repeated resolution of:

`COLOR_ONLY`

produces identical:

- candidate name
- candidate properties
- palette role
- resolved semantic values
- approval payload before DB-generated metadata

DB record IDs/timestamps are not expected to be identical across independent inserts.

Report exactly what was tested.

---

# 26. CLI

Add approval CLI following established patterns.

Expected:

`approve-focus-style --plan-id 7`

Reuse previous approval command architecture.

Do NOT add unrelated CLI commands.

Expected flow:

1. validate entry gate
2. query latest canonical
3. verify focus_style pending
4. resolve actual Human-selected candidate
5. validate candidate
6. preserve previous categories
7. mark ONLY focus_style APPROVED
8. append canonical row
9. update approved_visual_profile through established path
10. generate approval report
11. return final state

---

# 27. Approval Function

Prefer established naming pattern.

Expected conceptual function:

`run_focus_style_human_approval(...)`

Reuse:

- canonical selection
- readiness calculation
- persistence
- approved profile generation
- report architecture

Do NOT create new schema.

Do NOT create a parallel visual approval table.

---

# 28. Approval Report

Generate a dedicated report following existing convention.

Suggested:

`reports/focus_style_human_approval_2026-08-21.md`

Use actual repository date convention.

Report must distinguish:

REVIEW PROTOTYPE:
13-4C-16

from:

HUMAN APPROVAL:
13-4C-17

and explicitly state:

HUMAN SELECTED CANDIDATE:
COLOR_ONLY

FOCUS STYLE:
APPROVED

Include exact persisted values.

---

# 29. README

Update README only as necessary.

Expected minimal update:

add:

`approve-focus-style --plan-id 7`

Update visual approval status/count if README currently tracks it.

Expected after successful approval:

7 APPROVED / 8 PENDING

But derive from actual state.

Do not claim full profile approval.

Do not claim Stage 13-5 readiness.

---

# 30. PROJECT_STATE.md

Update PROJECT_STATE.md after successful persistence.

Expected orientation:

Current Major Stage:
13-4C Human Visual Review

Current Sub-stage:
13-4C-17 Focus Style Human Approval — completed

Canonical record:
<actual new id>

Approved categories:
7

Pending categories:
8

focus_style:
APPROVED

Selected Focus Style:
COLOR_ONLY

Exact properties:
actual persisted properties

Full Approved Visual Profile:
NO

Ready for Final Renderer Binding:
NO

Ready for Stage 13-5:
NO

Renderer:
NOT_STARTED

Next Human Visual Review:
`success_style`

or another remaining mandatory category if actual project ordering dictates otherwise.

Do not record future review as completed.

---

# 31. Test Baseline

Previous report states:

1020 passed
0 failed

This is historical information only.

Before implementation establish actual current test baseline.

After implementation run:

1. new Focus Style Approval tests
2. relevant visual design tests
3. full regression suite

Report:

- baseline
- new tests
- modified existing tests
- total tests
- passed
- failed
- warnings
- regressions

Do not claim test execution that did not happen.

---

# 32. External API Calls

Expected:

Gemini = 0
YouTube = 0
Video AI = 0
Image AI = 0
Font Network = 0

No external network/API work is required.

If any external call unexpectedly occurs:

report it.

Do not hide it.

---

# 33. WAV / MP4

Expected:

WAV GENERATED:
NO

MP4 GENERATED:
NO

Do not generate either.

---

# 34. Git Safety

There are accumulated uncommitted project changes.

Do NOT:

- reset
- clean
- stash
- revert unrelated work
- checkout over files
- delete untracked prompt/history files
- commit
- push

unless explicitly requested.

At completion show accurate git status summary.

---

# 35. Do Not Touch Historical Evidence

Preserve all earlier Human Review artifacts and reports.

Especially preserve:

`assets/generated/plan_7/render/focus_style_review/`

The Review prototype is evidence of what was presented before approval.

Do not rewrite:

`human_decision: null`

inside the historical 13-4C-16 manifest.

That value correctly describes the state at prototype-generation time.

13-4C-17 approval is a separate historical event.

---

# 36. Source-of-Truth Precedence

When conflicts occur, use this precedence:

1. actual current canonical DB state
2. canonical machine-readable artifacts
3. actual current repository code
4. actual 13-4C-16 review artifacts
5. direct Human decision supplied for this stage
6. PROJECT_STATE.md
7. README.md
8. historical completion reports
9. expectations written in this prompt

Exception:

The Human selection itself is supplied explicitly by the user for this stage:

`COLOR_ONLY`

Repository state determines what `COLOR_ONLY` means.

Do not let a stale project document overwrite the actual Human selection.

---

# 37. STOP Conditions

STOP before persistence if:

- canonical record is missing
- focus_style is already approved
- 13-4C-16 candidate definition cannot be found
- COLOR_ONLY does not exist
- actual COLOR_ONLY materially differs from reviewed artifact
- PRIMARY_FOCUS role is missing
- existing six approved categories do not match canonical state
- previous canonical history appears corrupted
- approval would require mutating historical rows
- approval pipeline would approve unrelated categories

Report evidence instead of guessing.

---

# 38. Expected Successful Transition

Conceptually:

BEFORE

Canonical:
11

Approved:
6

Pending:
9

focus_style:
PENDING_VISUAL_REVIEW

Human decision:
COLOR_ONLY


AFTER

Canonical:
<new append-only record, expected 12 but verify>

Approved:
7

Pending:
8

focus_style:
APPROVED

Human selected candidate:
COLOR_ONLY

Other pending categories:
unchanged

Full profile:
NO

Renderer binding:
NO

Stage 13-5:
NO

Renderer:
NOT_STARTED

---

# 39. Required Final Console Output

At successful completion print something equivalent to:

FOCUS STYLE HUMAN REVIEW:
APPROVED

HUMAN SELECTED CANDIDATE:
COLOR_ONLY

APPROVED FOCUS STYLE:
  focus_color_role: PRIMARY_FOCUS
  resolved_focus_color: #60a5fa
  highlight_box: false
  box_opacity: 0.0
  padding: 0
  underline: false

HUMAN DECISION SOURCE:
current conversation — option 1 / COLOR_ONLY

PREVIOUS CANONICAL RECORD:
<actual>

NEW CANONICAL RECORD:
<actual>

APPEND-ONLY:
YES

PREVIOUS ROW MUTATION:
NO

APPROVED CATEGORY COUNT:
<actual>

PENDING CATEGORY COUNT:
<actual>

EXISTING APPROVED CATEGORIES PRESERVED:
YES

UNRELATED CATEGORY AUTO-APPROVAL:
NO

FOCUS STYLE REVIEW ARTIFACTS:
UNCHANGED

APPROVED_VISUAL_PROFILE:
UPDATED

FULL APPROVED VISUAL PROFILE:
NO

READY FOR FINAL RENDERER BINDING:
NO

READY FOR STAGE 13-5:
NO

RENDERER:
NOT_STARTED

TEST BASELINE:
<actual>

TEST RESULT:
<actual>

REGRESSIONS:
<actual>

EXTERNAL API CALLS:
Gemini <actual>
YouTube <actual>
Video AI <actual>
Image AI <actual>
Font Network <actual>

WAV GENERATED:
NO

MP4 GENERATED:
NO

GIT COMMIT:
NO

GIT PUSH:
NO

NEXT:
Continue remaining Human Visual Review.
Do not start Stage 13-5.

---

# 40. Completion Report

Return:

## 13-4C-17. Focus Style Human Approval — 완료 보고

Include at minimum:

1. modified/added files
2. architecture/reused approval pattern
3. actual test baseline
4. final test count
5. new tests
6. modified existing tests
7. Plan ID
8. Visual Design version
9. Review/Approval stage
10. Canonical Visual Candidate
11. Human decision source
12. selected candidate
13. actual reviewed candidate definition
14. persisted Focus Style values
15. PRIMARY_FOCUS role/value
16. focus_style status before/after
17. previous canonical id
18. new canonical id
19. append-only YES/NO
20. previous row mutation YES/NO
21. existing approved category preservation
22. approved count before/after
23. pending count before/after
24. unrelated auto-approval YES/NO
25. approved_visual_profile update
26. focus_style_review artifact unchanged
27. previous review artifacts unchanged
28. production_blocks unchanged
29. speech_assets unchanged
30. generated_assets unchanged
31. render_specs unchanged
32. render_timelines unchanged
33. scene_layouts unchanged
34. source_text/display_text unchanged
35. pronunciation review unchanged
36. CB06 unchanged
37. CB07 ACTIVE/MUTED semantics unchanged
38. validation/integrity checks
39. negative test results
40. determinism result
41. total tests passed/failed
42. regressions
43. external API counts
44. WAV generated
45. MP4 generated
46. README update
47. PROJECT_STATE update
48. git commit
49. git push
50. bugs discovered
51. semantic debt discovered
52. limitations
53. unresolved critical
54. unresolved non-critical
55. Full Approved Visual Profile
56. Ready for Final Renderer Binding
57. Ready for Stage 13-5
58. Renderer status
59. next Human Review target
60. success criteria result

---

# 41. Success Criteria

13-4C-17 is complete only if:

- actual Human selection is COLOR_ONLY
- actual reviewed COLOR_ONLY definition is resolved from repository evidence
- only focus_style becomes APPROVED
- previous six approvals remain unchanged
- no unrelated pending category becomes approved
- exactly one append-only canonical record is created
- previous canonical record remains unchanged
- approved_visual_profile is updated consistently
- 13-4C-16 review artifacts remain unchanged
- historical manifest remains historical
- production content remains unchanged
- CB06 remains unchanged
- CB07 ACTIVE/MUTED semantics remain unchanged
- no external API calls are required
- no WAV is generated
- no MP4 is generated
- regression suite passes
- PROJECT_STATE reflects the new verified state
- Full Approved Visual Profile remains NO
- Renderer Binding remains NO
- Stage 13-5 remains NO
- Renderer remains NOT_STARTED

If any critical invariant fails:

STOP.

Do not partially persist the approval.

Report the exact discrepancy.