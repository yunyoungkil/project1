너는 기존 YouTube 채널 운영 자동화 프로젝트를 확장하는 시니어 백엔드/데이터 엔지니어다.

현재 프로젝트에는 이미 YouTube API를 이용해 **내 YouTube 채널 데이터를 조사·분석하는 기능**이 존재한다.

기존 코드를 먼저 충분히 분석하고, 중복 구현하지 말고 현재 구조와 패턴을 최대한 재사용하라.

이번 작업의 목표는 다음 기능을 추가하는 것이다.

# 최종 목표

한국 YouTube의 영어교육 시장을 자동 조사해서,

1. 시청자 고민/검색 키워드별 관련 영상 수집
2. 작은 또는 중간 채널에서 비정상적으로 성과가 좋은 영상 탐지
3. 채널 평소 조회수 대비 Outlier Score 계산
4. 조회수 대비 구독자 비율 계산
5. 업로드 날짜를 고려한 조회 속도 계산
6. Shorts와 Long-form을 분리 분석
7. 시장에서 성과가 좋은 문제·주제·제목 패턴 분석
8. 기존 내 채널 데이터와 시장 데이터를 결합
9. 다음에 제작할 가능성이 높은 콘텐츠 후보 TOP 3 추천
10. 매주 실행 가능한 리포트 생성

까지 자동화하는 것이다.

단순 경쟁채널 수집기가 아니라,

**시장 수요 × 경쟁 영상 이상치 × 내 채널 적합도**

를 분석하는 콘텐츠 리서치 시스템을 만들어라.

---

# 1. 먼저 기존 프로젝트 분석

코드를 수정하기 전에 프로젝트 전체 구조를 조사하라.

반드시 확인할 것:

- 현재 YouTube Data API 사용 위치
- YouTube Analytics API 사용 여부
- OAuth/API key 관리 방식
- 환경변수 구조
- DB 또는 JSON/CSV 저장 구조
- 현재 내 채널 데이터 수집 기능
- 스케줄러/워크플로우 구조
- LLM 호출 코드가 존재하는지
- report 생성 방식
- CLI 또는 API endpoint 구조
- 테스트 구조

현재 구현이 존재하는 기능은 재사용한다.

새로운 YouTube API client를 불필요하게 중복 생성하지 마라.

기존 기능을 깨뜨리지 않는다.

---

# 2. 디렉터리 구조

기존 프로젝트 스타일을 우선 따르되, 별도 모듈이 필요하다면 대략 다음 책임을 분리한다.

```text
research/
  keyword_pool
  youtube_search
  video_stats
  channel_baseline
  outlier_detector
  opportunity_score
  content_pattern_analyzer
  weekly_report
```

실제 디렉터리명과 언어/프레임워크는 기존 프로젝트에 맞춰라.

---

# 3. 시청자 고민 Keyword Pool

초기 데이터로 다음 한국 영어 학습자 고민을 사용한다.

각 고민은 category, problem, search_queries를 가질 수 있게 설계하라.

## reading

- 영어 왕초보 어디서부터 시작해야 하나
- 알파벳은 아는데 영어 단어를 못 읽는다
- 처음 보는 영어 단어를 읽을 수 없다
- 영어는 왜 철자대로 읽히지 않는가
- 영어 발음 규칙에는 왜 예외가 많은가
- 성인이 파닉스를 배워도 되는가
- 발음기호를 꼭 알아야 하는가
- 영어 모음은 왜 소리가 계속 바뀌는가
- 영어 묵음은 왜 생기는가
- 긴 영어 단어를 어디서 끊어 읽는가
- 영어 강세 위치를 어떻게 아는가
- 영어 강세를 틀리면 문제가 되는가

예시 search_queries:

```text
영어 단어 읽는 법
처음 보는 영어 단어 읽기
영어 철자 발음
영어 왕초보 읽기
성인 파닉스
영어 강세
```

## listening

- 원어민은 왜 이렇게 빨리 말하는가
- 아는 단어인데 원어민이 말하면 안 들린다
- 자막을 보면 아는데 자막 없이 안 들린다
- 영어 연음이 어렵다
- did you가 왜 디쥬처럼 들리는가
- what are you가 왜 다르게 들리는가
- gonna/wanna는 왜 쓰는가
- 영화나 미드는 왜 특히 안 들리는가

예시:

```text
영어 듣기 안되는 이유
원어민 영어 안 들림
영어 연음
미드 영어 듣기
원어민 영어 발음
```

## vocabulary

- 영어 단어를 외워도 자꾸 잊는다
- 아는 단어가 말할 때 생각나지 않는다

## speaking

- 문법은 아는데 말을 못한다
- 영어를 이해하지만 말을 못한다
- 머릿속에서 번역하느라 말이 느리다
- 틀릴까 봐 영어를 못한다

예시:

```text
영어 말이 안 나와요
영어 알아듣는데 말 못함
영어 회화 왕초보
```

## pronunciation

- R/L 발음 구별
- TH 발음
- 단어는 아는데 문장을 못 읽는다

## beginner

- 읽기부터 해야 하는가 회화부터 해야 하는가
- 성인 영어 공부 순서
- 영어 왕초보 공부법

Keyword Pool은 나중에 JSON/YAML/DB에서 쉽게 수정하거나 추가할 수 있도록 만든다.

코드 내부에 난잡하게 하드코딩하지 않는다.

---

# 4. YouTube Search

YouTube Data API를 이용해 각 검색어별 관련 영상을 수집하라.

가능하면 한국어 콘텐츠를 우선한다.

필요한 데이터:

```text
video_id
channel_id
title
description
published_at
thumbnail_url
duration
view_count
like_count
comment_count
channel_title
subscriber_count
channel_video_count
channel_view_count
matched_keyword
problem_category
```

중복 video_id는 제거한다.

검색 API quota를 낭비하지 않도록 캐시를 구현한다.

같은 검색어를 짧은 기간 내 반복 호출하지 않는다.

가능하면 기존 DB/cache 시스템을 활용한다.

검색 수, 결과 수, 실행 주기는 config로 변경 가능하게 만든다.

---

# 5. Long-form / Shorts 분리

Shorts와 일반 Long-form 영상의 조회수 특성이 다르기 때문에 반드시 분리한다.

가능한 API 데이터와 영상 duration 등을 이용해 합리적인 방식으로 분류한다.

완벽하게 Shorts 여부를 판별할 수 없는 경우:

```text
content_type:
  longform
  short
  unknown
```

등으로 처리하고 코드와 README에 판별 한계를 명시한다.

Long-form baseline과 Shorts baseline을 절대로 섞지 않는다.

---

# 6. Channel Baseline 계산

후보 영상이 발견되면 해당 채널의 최근 영상 데이터를 추가 수집한다.

가능하면 최근 20~30개 영상을 이용한다.

그러나 다음 조건을 적용한다.

- 분석 대상 영상과 같은 content_type끼리 비교
- Shorts와 Long-form 분리
- 지나치게 오래된 콘텐츠 제외 가능
- live stream 등 특수 콘텐츠는 가능하면 제외
- 분석 대상 영상 자체는 baseline 계산에서 제외

평소 조회수는 평균(mean)보다 중앙값(median)을 우선 사용한다.

계산:

```text
channel_median_views
channel_mean_views
```

둘 다 저장하되 Outlier 계산은 median을 기본으로 한다.

---

# 7. 핵심 지표 계산

## A. Subscriber Ratio

```text
subscriber_ratio =
video_view_count / subscriber_count
```

subscriber_count가 hidden 또는 0이면 null 처리한다.

오류를 만들지 않는다.

---

## B. Channel Outlier Ratio

가장 중요한 지표다.

```text
outlier_ratio =
video_view_count / channel_median_views
```

예:

```text
영상 조회수 = 100000
채널 최근 비슷한 영상 중앙값 = 2500

outlier_ratio = 40
```

즉 40X.

baseline이 너무 적거나 신뢰도가 낮으면 confidence를 낮게 표시한다.

---

## C. Views Per Day

```text
age_days =
max(1, today - published_at)

views_per_day =
view_count / age_days
```

---

## D. Engagement 참고 지표

가능하면:

```text
like_rate = likes / views
comment_rate = comments / views
```

단 YouTube API에서 데이터가 없거나 숨겨져 있는 경우 graceful하게 처리한다.

---

# 8. Outlier 등급

기본 기준:

```text
outlier < 2X       = normal
2X ~ 5X            = notable
5X ~ 10X           = strong
10X ~ 20X          = very_strong
20X 이상           = exceptional
```

config에서 변경 가능하게 만든다.

특히:

```text
5X 이상 → 저장할 가치 있음
10X 이상 → 중요 분석 대상
20X 이상 → 최우선 분석 대상
```

으로 사용할 수 있도록 한다.

---

# 9. 오래된 영상 편향 보정

오래된 영상이 무조건 높은 점수를 얻지 않게 한다.

단순 조회수만으로 ranking하지 않는다.

다음 요소를 함께 고려한다.

```text
outlier_ratio
subscriber_ratio
views_per_day
published_at
engagement
baseline_confidence
```

다만 지나치게 복잡한 머신러닝 모델은 만들지 마라.

해석 가능한 점수식을 사용한다.

---

# 10. Opportunity Score

최종적으로 각 영상에 `opportunity_score`를 계산한다.

점수는 0~100을 권장한다.

기본 개념:

```text
Opportunity =
Outlier Strength
+ Recency / Velocity
+ Subscriber Advantage
+ Problem Relevance
+ Engagement
```

Outlier Ratio에 가장 큰 가중치를 둔다.

예:

```text
outlier_strength: 40%
views_velocity: 20%
subscriber_ratio: 10%
engagement: 10%
keyword/problem relevance: 20%
```

정확한 공식은 구현 전에 합리적으로 설계하고 코드 주석과 문서에 남긴다.

극단적인 값 때문에 전체 점수가 망가지지 않도록 log normalization 또는 cap을 고려한다.

---

# 11. 작은 채널의 성공 영상 우선 탐색

대형 채널의 높은 조회수보다 다음 사례를 더 가치 있게 평가한다.

예:

```text
채널 평소 조회수: 2,000
특정 영상: 80,000
Outlier = 40X
```

반면:

```text
채널 평소 조회수: 500,000
특정 영상: 800,000
Outlier = 1.6X
```

라면 첫 번째 영상을 훨씬 높은 가치로 평가한다.

우리가 찾고 싶은 것은 단순 인기 영상이 아니라,

**채널 규모와 평소 성과를 뛰어넘은 영상**

이다.

---

# 12. TOP 20 생성

각 실행 결과에서 다음을 생성한다.

## 전체 Outlier TOP 20

각 행:

```text
rank
title
channel
video_url
thumbnail
published_at
views
subscriber_count
channel_median_views
subscriber_ratio
outlier_ratio
views_per_day
opportunity_score
matched_keyword
problem_category
```

---

# 13. 제목 및 콘텐츠 패턴 분석

LLM 기능이 프로젝트에 이미 있다면 기존 LLM client를 재사용한다.

TOP 영상의 다음 요소를 분석한다.

```text
시청자의 실제 문제
제목이 건드리는 감정
제목의 클릭 장치
호기심 장치
결과 약속
초보자에게 매력적인 이유
질문형인지
부정형인지
이유형인지
결과형인지
숫자형인지
공포/실수 회피형인지
```

예:

```json
{
  "viewer_problem": "아는 단어인데 원어민이 말하면 안 들림",
  "title_pattern": "문제 + 이유",
  "hook": "왜 안 되는가에 대한 호기심",
  "promise": "영어 듣기의 원인을 이해하게 해줌"
}
```

제목을 그대로 베끼는 기능은 만들지 않는다.

패턴만 추출한다.

---

# 14. 내 채널 데이터와 결합

기존 프로젝트에서 수집하고 있는 내 채널 데이터를 사용한다.

가능하면 다음 지표를 활용한다.

```text
내 영상 주제
조회수
CTR
average_view_duration
average_percentage_viewed
watch_time
subscriber_gain
impressions
traffic_source
```

사용 가능한 데이터만 활용하고 없는 데이터는 가정하지 않는다.

시장 데이터와 내 채널 데이터를 연결해:

```text
market_demand_score
my_channel_fit_score
```

를 계산한다.

예:

```text
시장:
"아는 단어인데 원어민이 말하면 안 들리는 이유"
관련 Outlier 영상 다수
평균 Outlier 높음

내 채널:
슈와/연음/발음 관련 영상 retention이 상대적으로 높음

→ 높은 my_channel_fit
```

---

# 15. 최종 Content Opportunity Score

주제별 최종 점수를 생성한다.

개념:

```text
Content Opportunity =
Market Opportunity
× My Channel Fit
```

또는 해석하기 좋은 weighted score 방식으로 구현해도 된다.

중요한 것은:

**시장에서는 잘되지만 내 채널과 맞지 않는 주제**

와

**내 채널에는 맞지만 시장 수요가 약한 주제**

를 구분할 수 있어야 한다.

---

# 16. 다음 콘텐츠 TOP 3 추천

최종적으로 다음 제작 후보를 TOP 3까지 제안한다.

각 후보에는:

```text
주제
해결하는 시청자 고민
시장 근거
관련 Outlier 영상 수
대표 Outlier Ratio
내 채널 적합 근거
Content Opportunity Score
추천 롱폼 제목 3개
추천 썸네일 문구 2~3개
추천 Shorts 아이디어 1~2개
```

를 포함한다.

제목은 경쟁 영상 제목을 복제하지 않는다.

우리 채널에 맞게 새로 작성한다.

---

# 17. 우리 채널의 방향

추천 콘텐츠는 다음 방향에 우선순위를 둔다.

채널명:

```text
이해하면 쉬운 영어
```

핵심 시청자:

```text
영어를 배운 적은 있지만 여전히 영어가 어려운 성인 왕초보
알파벳은 알지만 단어 읽기, 듣기, 실제 발음에서 어려움을 겪는 학습자
```

핵심 철학:

```text
영어는 외울수록 어려워지고, 이해할수록 쉬워집니다.
```

주요 콘텐츠 영역:

```text
영어 읽기 원리
철자와 발음
강세
schwa
연음
약화
묵음
실제 원어민 발음
영어가 안 들리는 이유
처음 보는 단어 읽기
```

콘텐츠 추천이 너무 일반적인 회화/문법 채널 방향으로 흘러가지 않게 한다.

---

# 18. Weekly Report

매주 실행 가능한 리포트를 생성한다.

예:

```markdown
# Weekly YouTube Market Research

기간:
2026-xx-xx ~ 2026-xx-xx

## 1. 이번 주 시장 Outlier TOP 10

1. 영상 제목
   - Channel:
   - Views:
   - Channel baseline:
   - Outlier: 44.9X
   - Opportunity: 91/100
   - Viewer problem:
   - Title pattern:

...

## 2. 반복적으로 나타나는 시청자 고민

1. 아는 영어인데 안 들림
2. 처음 보는 단어를 못 읽음
3. 영어 공부 순서를 모름

## 3. 이번 주 강한 제목 패턴

- "왜 ~할까?"
- "~해도 안 되는 이유"
- "알고 있는데 왜 ~?"
- "처음 보는 ~도"

## 4. 내 채널과의 교집합

시장 수요:
...

내 채널 성과:
...

## 5. 다음 콘텐츠 추천 TOP 3

### #1
주제:
왜 아는 영어도 원어민이 말하면 안 들릴까?

시장 근거:
...

내 채널 근거:
...

추천 제목:
1.
2.
3.

썸네일:
1.
2.

Shorts:
1.
2.

## 6. 이번 주 최우선 제작 추천

Long-form:
...

Shorts:
...

이유:
...
```

---

# 19. 실행 주기

초기에는 API quota를 고려해 keyword category를 순환 실행할 수 있게 한다.

예:

```text
Monday    reading
Tuesday   listening
Wednesday beginner
Thursday  speaking
Friday    전체 점수 재계산 + Weekly Report
```

그러나 스케줄은 코드에 고정하지 말고 config로 관리한다.

수동으로 전체 실행할 수도 있어야 한다.

---

# 20. API quota 관리

YouTube API quota가 중요하다.

특히 search endpoint를 무분별하게 반복 호출하지 않는다.

반드시:

- 검색 결과 cache
- video 통계 batch 요청
- channel 통계 batch 요청
- 이미 수집한 channel baseline 재사용
- 일정 시간 이내 동일 검색 재호출 방지
- API call logging
- 실행당 예상/실제 quota 사용량 기록

을 고려한다.

가능하면 API 요청 횟수를 최소화한다.

---

# 21. 데이터 저장

기존 프로젝트 저장 방식을 우선 사용한다.

필요하면 다음 개념을 저장한다.

```text
keywords
search_runs
videos
channels
channel_baselines
video_metrics
outlier_scores
topic_opportunities
weekly_reports
```

같은 영상/채널을 매번 새로 중복 저장하지 않는다.

시간에 따른 조회수 변화는 필요하면 snapshot 형태로 보존한다.

---

# 22. 오류 처리

다음을 안전하게 처리한다.

```text
구독자 수 숨김
좋아요 숨김
댓글 비활성
삭제된 영상
비공개 영상
API quota 초과
API 일시 오류
채널 최근 영상 부족
Short/Long 판별 불확실
0 division
중복 영상
missing fields
```

한 영상 때문에 전체 workflow가 실패해서는 안 된다.

---

# 23. 테스트

핵심 계산 로직에는 unit test를 작성한다.

특히:

```text
median baseline
subscriber_ratio
outlier_ratio
views_per_day
opportunity_score
content type separation
missing subscriber handling
zero baseline handling
```

을 테스트한다.

가능하면 실제 API를 호출하지 않는 mock 기반 테스트도 작성한다.

---

# 24. CLI 또는 실행 명령

기존 프로젝트 방식에 맞춰 아래와 비슷한 실행 방법을 제공한다.

예:

```bash
research keywords
research search --category reading
research search --category listening
research analyze
research top --limit 20
research report weekly
research run-all
```

실제 명령은 현재 프로젝트 구조에 맞게 설계한다.

---

# 25. 중요: 먼저 계획을 보여주고 구현

바로 대규모 코드를 작성하지 마라.

먼저:

1. 기존 프로젝트 구조 분석
2. 현재 재사용 가능한 코드 목록
3. 추가해야 할 파일
4. 수정해야 할 파일
5. 데이터 흐름
6. YouTube API 호출 흐름
7. quota 예상
8. scoring 공식
9. 위험 요소/제약

을 간단히 정리하라.

그 후 바로 구현을 진행하라.

불필요한 질문은 하지 말고 기존 코드와 합리적인 기본값을 이용해 진행한다.

---

# 26. 구현 원칙

- 기존 코드 스타일 준수
- 기존 기능 깨뜨리지 않기
- 최소 변경 우선
- 중복 코드 금지
- secrets/API key 코드에 하드코딩 금지
- 설정값 config/env로 분리
- type safety가 가능한 환경이면 type 사용
- 함수 책임 작게 유지
- 계산 공식은 쉽게 검증 가능하게 작성
- 마법 숫자 최소화
- README 업데이트
- 필요한 migration이 있다면 제공
- dependency 추가는 최소화

---

# 27. 최종 완료 보고

구현이 끝나면 다음 형식으로 보고하라.

```text
1. 추가된 기능
2. 변경된 파일
3. 새로 추가된 파일
4. 데이터 흐름
5. Outlier 계산 방식
6. Opportunity Score 계산 방식
7. API quota 절약 방법
8. 실행 방법
9. 테스트 결과
10. 실제 예시 실행 결과
11. 아직 존재하는 한계
12. 다음 단계 추천
```

가능하면 실제 프로젝트의 기존 YouTube API 인증 정보를 사용해 작은 범위의 테스트 실행까지 수행하고 결과를 검증하라.

단, 비용이나 quota를 과도하게 소비하는 대량 API 실행은 하지 않는다.

---

# 핵심 성공 조건

이 기능이 성공한 상태는 단순히 인기 영상을 보여주는 것이 아니다.

다음 질문에 자동으로 답할 수 있어야 한다.

> "지금 한국 영어교육 YouTube에서 작은 채널도 비정상적으로 조회수가 터지고 있는 시청자 고민은 무엇인가?"

그리고 그 다음 질문:

> "그중에서 내 채널 '이해하면 쉬운 영어'가 다음 영상으로 만들었을 때 가장 승산 높은 주제는 무엇인가?"

최종적으로 매주:

```text
시장 Outlier
+
시청자 고민
+
내 채널 실제 성과
=
다음 제작 우선순위
```

가 자동으로 나오게 만들어라.